interface:
  display_name: "Deployment Environment Secret Isolation"
  short_description: "Prevent and recover cross-environment credential writes"
  default_prompt: "Use $deployment-environment-secret-isolation to verify exact deployment, credential-step, and secret targets across environments."
